/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    domains: ["img.kurio.network", "images.pexels.com"],
  },
  /* config options here */
};

module.exports = nextConfig;
